import { writeFile } from "fs/promises";

export const formatTitle = (id: string, name: string): string => {
  return '';
};

export const createDir = async () => {

};

export const createReadme = async (id: string, name: string) => {};

export const createTemplate = async () => {};
